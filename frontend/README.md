# Projeto base Angular 19

Este repositório contém um esqueleto mínimo de um projeto Angular 19.

Instalação e execução:

```bash
cd frontend
npm install
npx ng serve --open
```

Observação: é recomendado ter @angular/cli local ou usar `npx ng`.
# Frontend Angular

Starter Angular project deve ser criado pelo candidato.